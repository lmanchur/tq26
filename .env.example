# Copy to .env for local overrides. .env is gitignored.
# GOREST_TOKEN is a real secret — never commit a filled value.
GOREST_TOKEN=

# BearStore demo credentials are non-sensitive; plaintext in repo/CI is OK.
BEARSTORE_USERNAME=tq26authsetup
BEARSTORE_PASSWORD=Tq26Auth!Setup9
